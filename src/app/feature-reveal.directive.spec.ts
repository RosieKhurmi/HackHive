import { FeatureRevealDirective } from './feature-reveal.directive';

describe('FeatureRevealDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureRevealDirective();
    expect(directive).toBeTruthy();
  });
});
